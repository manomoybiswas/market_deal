<% content_for :title, "Categories" %>
<div class="page-wrapper font-robo">
  <div class="wrapper wrapper--w960">
    <div class="card card-2">
      <div class="row">
        <div class = "col-sm-0 col-md-2 card-heading"></div>
        <div class="col-sm-12 col-md-10">
          <div class="card-body">
            <h1 class="title text-center">Product Catagory List</h1>
            <div class="row">
              <div class="col-12 col-sm-6 text-center">
                <%= link_to  "Create Category", new_product_category_path, class: "btn btn-primary btn-lg" %>
              </div>
              <div class="col-12 col-sm-6 text-center">
                <%= link_to  "Back", admin_dashboard_path, class: "btn btn-secondary btn-lg" %>
              </div>
            </div>
            <br/>
            <table class="table table-striped table-bordered table-hover table-dark">
              <caption class="text-center"> List of Employees</caption>
              <thead>
                <tr class="text-center">
                  <th scope="col">#</th>
                  <th scope="col"> Categories</th>
                  <th scope="col">Edit</th>
                  <th scope="col">Delete</th>
                </tr>
              </thead>
              <% if @product_categories.present? %>
                <tbody>
                  <% @product_categories.each do |category|%>
                    <tr>
                      <th scope="row"><%= @product_categories.index(category)+1 %></th>
                      <td><%= category.category_name%></td>
                      <td class="text-center">
                        <%= link_to "", edit_product_category_path(category), class:"fa fa-edit"%>
                      </td>
                      <td class="text-center">
                        <%= link_to "", product_category_path(category), data: {confirm: "Are you sure?" }, method: :delete, class:"fa fa-trash"%>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              <% end %>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>